# knowledge-button
A basic HTML button attached to a JS function that, when clicked, opens a new tab on a random Wikipedia article.

The attached file is a template. Copy/paste the sections into your relevant HTML tags, or modify it as you wish.
